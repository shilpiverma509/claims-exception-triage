# Decision Log — Claims Exception Triage Assistant

Every ratified build decision, with the rejected alternative and Shilpi's rationale.
This file is the raw script for the demo narrative's "build decisions and tradeoffs" third.

| Date | Decision | Alternative rejected | Rationale (Shilpi's words) |
|------|----------|----------------------|----------------------------|
| 2026-08-22 | Track 1: Claims Exception Triage over Prior Auth agent | Multi-step PA intake agent | Packet says choose smaller build and show it well; hours go to evals/controls, not plumbing |
| 2026-08-22 | Fixed root-cause taxonomy (7 causes) as enums; LLM must pick from list | Free-form root-cause text | These 7 are common, real-world reasons claims pend, not invented at random. The taxonomy doesn't need to cover every possible case — a claim that doesn't fit cleanly gets low confidence and fails safe to human_review instead of being force-guessed. It fails safely rather than guessing. |
| 2026-08-22 | Money as integer cents everywhere | Floats | _pending Shilpi's ratification_ |
| 2026-08-22 | ~30% of claims get generic pend codes (P99/P00) | All claims carry accurate codes | A code like P99 is generic and holds little meaning by itself. The LLM doesn't just read the note — it gathers evidence from history, the PA registry, and contract rates, then compares that evidence against the note, which a pend-code lookup table could never do. |
| 2026-08-23 | Project renamed ClaimsTriageAssistance; target path ~/Documents/workspace/projects/ClaimsTriageAssistance | keep cloud-only name | match Shilpi's workspace layout |
| 2026-08-23 | Mock enterprise systems as JSON fixtures with adapter-style lookup() | hardcode evidence into prompts | production swap = adapter change; evidence citable |
| 2026-08-23 | 3 disprovable PA traps planted | all-consistent evidence | tests whether model checks registry vs trusting the note |
| 2026-08-23 | Redaction runs before every LLM call, findings audited | trust synthetic data | control must exist & be tested regardless of data |
| 2026-08-23 | Ranking policy: urgency desc, then billed dollars desc as tie-break (pipeline, baseline, evaluate, UI all share it) | Sort by urgency alone | Guard's SLA floor flattened 19 claims to 95; arbitrary tie order buried $42k claims under $45 ones — critical-recall@10 was 0.375 by luck of insertion order, 1.00 with the tie-break. Metric measured tie-breaking, not ranking. |
| 2026-08-23 | contract_rates fixture strips ALL provider+cpt keys belonging to pricing_mismatch claims (post-pass) | leave shared-key collisions | Rate keys are shared across claims; non-pricing claims filled 7 "deliberate gaps," contaminating the evidence. Found via the single live-run root-cause miss (CLM-2026-8048): Claude flagged the note/evidence contradiction and got penalized for trusting evidence that was wrong. Failure-mode catalog entry: evidence keyed too coarsely = cross-claim contamination. |
| 2026-08-24 | Feedback loop built for uses 1-3 (regression cases, calibration, prompt material); inference-time retrieval deliberately excluded | full RAG loop over corrections | Uses 1-3 need only a corrections table and a script. Feeding corrections into an inference-time lookup risks self-reinforcement (uncorrected errors become precedent) and needs a re-validation set the loop never touches — out of scope for v1, documented as the production path. |
| 2026-08-24 | Approvals are NEVER emitted as labels; corrections only | treat approve as a positive label | An approval may mean "correct" or may mean rubber-stamping (failure mode #8). Using them as ground truth feeds the system its own output. Approvals count in calibration denominators only. Pinned by test. |
| 2026-08-24 | UI captures corrected ROOT CAUSE, not just corrected queue | keep queue-only reassign | Routing derives from cause via the taxonomy map, so queue-only feedback records the symptom and loses the diagnosis — the part that can become a regression case or prompt exemplar. |
| 2026-08-24 | Feedback cases kept in their own file, labelled a regression suite | merge into dev_labels.json | Every case is one the system got wrong, so the set is adversarial by construction. Averaging it with the dev set produces a meaningless accuracy number. |
| 2026-08-24 | Severity on feedback cases derived from dollars+SLA, not asked of the analyst | add a severity picker to the review UI | Severity means "cost of being wrong" — a function of claim facts, not an opinion. Every extra field on the review screen is friction that reduces how much feedback arrives at all. |
| 2026-08-24 | Reassign that keeps the same cause is not a diagnostic correction | count every reassign as a correction | Found running the loop end-to-end: it produced a "coding_error -> coding_error" confusion pair and an exemplar reading "X, NOT X". Such cases count for calibration but never become regression cases or exemplars. |
| 2026-08-24 | recommend_floor() reports the COST of a floor and declines when evidence is thin | recommend a floor from any data | A floor that escalates everything is perfectly safe and perfectly useless; a recommendation without its escalation cost is not actionable. With errors spread across all buckets, no floor is defensible — say so rather than guess. |
| 2026-08-24 | Built TF-IDF + cosine-similarity + kNN retrieval over a resolved-claims archive, with a two-tier router skipping the LLM on strong precedent | Full LLM-only pipeline (no retrieval) | Built in response to self-generated notes after re-reading the instruction packet, mistaken at the time for feedback from an assigned reviewer. Answered "learn from the past," "vector search," and "cheap pre-filter" in one component; kNN chosen over a trained classifier because training on 50 claims from ~14 templates would learn the generator, not claims processing. |
| 2026-08-25 | Reverted retrieval/router entirely; project ships LLM-only | Keep retrieval as a stretch feature | Re-read the actual instruction packet: the AI-evidence bullet requires "retrieval **or** data approach" (an or) — the existing data approach (synthetic generation + mock systems) already satisfies it. The packet also states twice that a smaller, well-demoed build beats an ambitious one ("when in doubt about scope, choose the smaller build"). The ML vocabulary (vectors, cosine similarity, kNN) was not something Shilpi could confidently defend cold four days before the demo. Kept out of scope rather than risk defending something shakily. |
| 2026-08-25 | Lead AI-evidence story with baseline-vs-LLM comparison (72% vs 100%), not "v1 failed the PA traps, v2 fixed them" | Keep the original "trap-then-fix" prompt-version narrative | Checked the actual saved run files: v1 already answered all 3 planted PA-registry traps correctly, because the trap notes never actually contained PA-related language to mislead it — the trap was built but never triggered. Telling the "v1 failed" story would contradict Shilpi's own saved data if a reviewer checked it. The real, measured, defensible evidence of LLM lift is the rule-based baseline scoring only 72% (missing CLM-2026-8020) versus every LLM prompt version scoring 100%. VERIFY_BLOCK kept in v2/v3 prompts, described honestly as defense-in-depth for a misleading-note scenario, not as a fix for an observed failure. |
| 2026-08-27 | Sealed 25-claim eval run exactly once with frozen prompt V3; result reported verbatim (96% root-cause, 96% routing, 100% recall@10, 0.826 Spearman) | re-run after seeing the miss | The whole value of a held-out set is that it is touched once. Re-running after inspecting the failure would convert it into a second dev set and the number would mean nothing. |
| 2026-08-27 | Sealed-set miss CLM-2026-8050 traced to OUR prompt, not the model | log it as a model error | V3 states "a missing contract_rate is positive evidence FOR pricing_mismatch". The rate was missing, so the model applied the rule as written and chose pricing over eligibility. The instruction is too absolute. Fix queued for the next prompt version, NOT applied now — changing a frozen prompt would invalidate the sealed result. |
| 2026-08-27 | Default prompt changed v1 -> v3 in pipeline.py and Makefile | leave default at v1 | v3 is the frozen prompt used for the sealed run. Leaving the default at v1 meant `make triage` would silently run a different prompt than the one the reported numbers came from. |
| 2026-08-27 | Prompt versions V1/V2/V3 retained (ratified by Shilpi) | collapse to a single prompt before submission | PRD line 66 lists the prompt-iteration log as required "evaluation discipline" evidence. The system already runs one prompt at a time; the versions are the documented history of how it was reached, not parallel complexity. |
